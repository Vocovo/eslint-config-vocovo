// There should be no errors with this file

function foo() {
  const bar = 'Hello world'

  return bar
}

foo()

describe('Testing jest', () => {
  beforeEach(() => {
    // do something
  })

  it('wibbles', () => {
    // do something
  })
})
